{
  "name": "authorization",
  "version": "0.0.0",
  "description": "",
  "main": "server.js",
  "scripts": {
    "postinstall": "npm-run-all --serial server-install client-spa-install",
    "start": "npm-run-all --parallel server-start client-spa-start",
    "server-install": "cd api && npm install",
    "server-start": "cd api && npm start",
    "client-spa-install": "cd app-spa && npm install",
    "client-spa-start": "cd app-spa && npm start"
  },
  "author": "Auth0",
  "license": "MIT",
  "dependencies": {
    "npm-run-all": "^4.0.1"
  }
}
